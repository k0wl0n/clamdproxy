global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'clamdproxy'
    static_configs:
      # docker.for.mac.localhost is the IP address of the Docker host on macOS.
      - targets: ['docker.for.mac.localhost:2112']